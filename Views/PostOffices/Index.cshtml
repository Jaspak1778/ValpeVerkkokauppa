@model IEnumerable<ValpeVerkkokauppa.Models.PostOffice>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PostOffice1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PostOffice1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Zipcode }) |
            @Html.ActionLink("Details", "Details", new { id=item.Zipcode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Zipcode })
        </td>
    </tr>
}

</table>
