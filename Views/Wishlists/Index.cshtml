@model IEnumerable<ValpeVerkkokauppa.Models.Wishlist>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Users.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Users.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WishlistID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WishlistID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WishlistID })
        </td>
    </tr>
}

</table>
